{
  "name": "gitlab-event-producer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CrewStation 内置接入容器：校验 GitLab webhook 密钥，归一化成平台事件类型，以稳定去重键投给 cs-events",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "4.13.7"
  },
  "devDependencies": {
    "@types/bun": "1.4.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
