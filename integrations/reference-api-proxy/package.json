{
  "name": "reference-api-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CrewStation 参考接入容器：对接本机测试 GitLab 的纯转发 APIProxy，其他代理从这里复制",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "4.13.7"
  },
  "devDependencies": {
    "@types/bun": "1.4.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
