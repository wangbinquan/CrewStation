apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs-session
  namespace: crewstation-system
  labels: { app.kubernetes.io/name: cs-session, app.kubernetes.io/part-of: crewstation, app.kubernetes.io/managed-by: crewstation, crewstation.io/project: platform, crewstation.io/service: cs-session, crewstation.io/workload: platform }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: cs-session }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cs-session, app.kubernetes.io/part-of: crewstation, app.kubernetes.io/managed-by: crewstation, crewstation.io/project: platform, crewstation.io/service: cs-session, crewstation.io/workload: platform }
    spec:
      serviceAccountName: crewstation-control
      containers:
        - name: cs-session
          image: cs-control-plane:dev
          imagePullPolicy: IfNotPresent
          args: ["apps/cs-session/src/main.ts"]
          ports: [{ name: http, containerPort: 8083 }]
          envFrom:
            - configMapRef: { name: crewstation-env }
            - secretRef: { name: crewstation-secrets }
          env:
            - { name: POD_IP, valueFrom: { fieldRef: { fieldPath: status.podIP } } }

          readinessProbe: { httpGet: { path: /healthz, port: 8083 }, periodSeconds: 5 }
          livenessProbe: { httpGet: { path: /healthz, port: 8083 }, periodSeconds: 15, initialDelaySeconds: 10 }
          resources: { requests: { cpu: 100m, memory: 256Mi }, limits: { cpu: "1", memory: 1Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: cs-session
  namespace: crewstation-system
spec:
  selector: { app.kubernetes.io/name: cs-session }
  ports: [{ name: http, port: 8083, targetPort: 8083 }]
