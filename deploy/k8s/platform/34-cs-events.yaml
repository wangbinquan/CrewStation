apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs-events
  namespace: crewstation-system
  labels: { app.kubernetes.io/name: cs-events, app.kubernetes.io/part-of: crewstation, app.kubernetes.io/managed-by: crewstation, crewstation.io/project: platform, crewstation.io/service: cs-events, crewstation.io/workload: platform }
spec:
  replicas: 1
  # 本机单节点的 CPU 请求已被占满，滚动更新的新 Pod 会一直 Pending（2026-09-16 cs-api 卡死时重启等了 6 分钟）；单副本直接重建。
  strategy: { type: Recreate }
  selector:
    matchLabels: { app.kubernetes.io/name: cs-events }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cs-events, app.kubernetes.io/part-of: crewstation, app.kubernetes.io/managed-by: crewstation, crewstation.io/project: platform, crewstation.io/service: cs-events, crewstation.io/workload: platform }
    spec:
      serviceAccountName: crewstation-control
      containers:
        - name: cs-events
          image: cs-control-plane:dev
          imagePullPolicy: IfNotPresent
          args: ["apps/cs-events/src/main.ts"]
          ports: [{ name: http, containerPort: 8084 }]
          envFrom:
            - configMapRef: { name: crewstation-env }
            - secretRef: { name: crewstation-secrets }
          env:
            - { name: POD_IP, valueFrom: { fieldRef: { fieldPath: status.podIP } } }

          readinessProbe: { httpGet: { path: /healthz, port: 8084 }, periodSeconds: 5, timeoutSeconds: 5 }
          livenessProbe: { httpGet: { path: /healthz, port: 8084 }, periodSeconds: 15, initialDelaySeconds: 10, timeoutSeconds: 5 }
          resources: { requests: { cpu: 100m, memory: 256Mi }, limits: { cpu: "1", memory: 1Gi } }
---
apiVersion: v1
kind: Service
metadata:
  name: cs-events
  namespace: crewstation-system
spec:
  selector: { app.kubernetes.io/name: cs-events }
  ports: [{ name: http, port: 8084, targetPort: 8084 }]
