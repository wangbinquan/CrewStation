{
  "name": "@crewstation/task-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "任务容器镜像与 TaskRunner：只依赖 contracts、kernel、ws、agent-drivers；进另一个镜像，不得引入控制面代码",
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@crewstation/agent-drivers": "workspace:*",
    "@crewstation/contracts": "workspace:*",
    "@crewstation/kernel": "workspace:*",
    "@crewstation/ws": "workspace:*",
    "@xterm/addon-serialize": "0.14.0",
    "@xterm/headless": "6.0.0",
    "ajv": "8.20.0"
  }
}
