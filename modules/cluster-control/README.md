# cluster-control（L2）

RFC-025 调和器：受管 Kubernetes 对象的观测映射、按期望渲染与改回（路由、限流中间件、命名空间、额度、网络策略）、按 UID 回收、孤儿回收与旧对象收编（收编目前只出空跑报告）

模板与规则见 `docs/engineering/repository-structure.md` §3；用例放哪、必须带哪些用例见 `docs/engineering/testing.md`。
