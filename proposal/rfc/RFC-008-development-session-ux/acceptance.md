# RFC-008 实现与验收记录

> 2026-09-20。作者已批准实施并提交上库。以下区分代码回归与真实部署，设计附件不作为实现证据。

## 实现

- 一级导航固定为 CLI 工作区、预览、代码、变更、数据访问、会话与环境。个人工作区和窗口名册仅位于 CLI 页。
- 数据申请与审批、编辑器以隐藏挂载保留输入；离开项目继续使用统一未保存确认。原生终端切页只分离显示连接，不结束执行。
- 顶栏保留状态、分支、准备发布。异常区区分平台准备、页面通道、环境失联、协议拒绝、恢复中和已失败；每种状态都有检查、日志或恢复入口。
- 算力读取失败或不可用可刷新，管理员可进入档位管理；执行权限与数据访问申请分别说明。有窗口后才显示排布操作。
- 结构化 connectionIssue 从 task-runtime 经端口投影至开发会话；新 Runner 握手成功清除旧故障。页面手动重连保留游标、订阅和草稿，拒绝未完成命令，不自动重发。
- 协议拒绝恢复沿用保卷作业：固定任务、修订、原因、Pod UID、PVC UID、套餐和请求编号；保留已有额度，旧令牌立即失效；失败补偿保留原卷并只释放一次额度。

## 自动化证据

- 控制面旧协议恢复、失败恢复、补偿与任务流定向测试：初始新用例复现失败，修复后恢复用例通过。手动重连用例确认旧连接迟到消息不生效，未完成写入不重放。
- 工作台全目录回归：436 pass / 0 fail，67 文件；六页导航、跨页代码与申请草稿、旧深链、浏览器返回、双击创建、后台活动、两个独立 Runner 等均通过。
- 结构和两套类型检查通过；控制面镜像 `8d9820c54f01…`、工作台镜像 `2ab5a141fa70…` 构建成功。
- 完整门禁首次在设计附件的未使用变量处停止；删除该变量后 `bun run check` 通过：1631 pass / 5 skip / 0 fail，1636 tests / 266 files / 185.53s。跳过的是需专门配置真实模型/平台环境的现有用例，不计作成功。

## 实机对象与前置证据

- demo 项目 `prj_01a090f72dea7000b5ff6d3841cbaf7b`；任务 `tsk_01a0954107447000b7936485fb80d15d`。
- 原 Pod `cs-demo/task-01a095410744`，UID `170ae869-fe9b-4bd8-9779-4b830cbc2e85`，Running，Runner 协议 1 被平台协议 2 拒绝。
- 原工作卷 `task-01a095410744-work`，UID `e5ef3ab4-0752-4195-87ac-e379962fc98f`，Bound / 10Gi。
- 恢复前 HEAD `9fd01fc08465c44ddf182bcd35fbec7a7b5b887c`，工作树干净，无未推送分支提交；另存全部非 .git / node_modules 文件摘要，供恢复后比对。
- 本机四个待更新平台部署均为 Recreate；API、controller、session 各 100m / 256Mi，console 50m / 64Mi。逐个替换保持原预约值，不增加副本。

## 真实恢复与交互（2026-09-20）

- 存量旧数据还会遗留 `connected=true`；已让协议拒绝投影优先，并为这个真实存量形态加入回归。当前成功握手仍原子清除拒绝记录。
- 实机另复现点击触发活动心跳使恢复检查修订过期；新增测试先红后绿。`touch` 只更新 `lastActivityAt`，环境状态与连接变化仍更新修订。运行时全套 **65 pass / 0 fail / 493 assertions**。
- 恢复作业 `e4e5aa6e-256b-4d6d-8119-d3af576194bc`，`09:01:09.959Z → 09:01:23.148Z`，13.189 秒后 ready。新 Pod `task-01a095410744-r-e4e5aa6e256b`，UID `e60060ac-64dd-4c3f-ab3c-449a11832d2c`；旧 Pod 已替换。环境选用已有 `rfc006-agent` 套餐（150m / 1Gi / 2Gi），原工作卷仍 10Gi。
- 恢复前后 taskId、PVC UID、HEAD 一致，工作树均干净且没有未推送提交。原有 585 个文件中 584 个摘要相同，仅 `.bun/install/cache/544aad29bf256d77.npm` 索引变化；新增 568 个文件均在 `.bun/install`。没有源文件丢失或修改，不能将缓存变化说成全部文件逐字相同。
- 原故障页实际走完检查、套餐选择、确认、排队、新环境已连接，旧协议告警自动消失。一次点击创建 CLI，执行环境 `tsk_01a0be0e9f8c70009ca39cae0f7bc398` Running，与开发容器分离。Agent `agt_01a0be0e9f8c700189d82ca9cbcce0e5`，终端 `pty_01a0be0e9f8c7002a238e76437f0caa4`，trace `01a09541074470018d04e65ae1a52399`。CLI `cce0e5` 经原生输入返回 `RFC008_OK_e4e5aa6e`，该回合 36.8 秒；未要求工具或文件操作。
- 浏览器真实读取 README，添加验收标记，切到数据页再返回，草稿保留；保存后“未提交文件”由 0 变为 1。随后通过编辑器恢复原文并保存，容器 Git 干净，README SHA-256 `0e54bf847caa7a68f24d50c63c112bf00edf3379c1cedba3e008793a98bbf209` 与恢复前完全相同。
- 预览显示“就绪”，内嵌页面实际显示 demo 的 development 环境和当前身份；未调用样例应用的发送功能。
- 数据申请理由跨数据／环境页保留；释放检查显示未保存申请提醒，取消后原稿仍在。仅检查并取消，没有申请生产访问或释放环境。
- 最终前端真实 1280×720 四窗各 547×222px，首窗 y≈199px；390×844 真实页面 iframe 下六个功能页的 `clientWidth=scrollWidth=390`。英文桌面、深色窄屏视觉核对；Home/End 切换页签并移动焦点正确。临时 iframe 只用于固定尺寸和主题，不注入业务状态或样式。

- 个人工作区实际完成新建、命名、CLI 移入再移回、关闭临时工作区；始终只有一个新 CLI，原终端输出保留。验收用临时工作区已清理，原 RFC-006 验收项目的横排／最大化布局已还原。
- 服务切换期间验证了故障可见和自动恢复；修复普通失联夹带旧成功 message 的矛盾提示。多浏览器页还复现远端布局覆盖当前 URL 视图、同地址无法返回 CLI，新增回归先红后绿，明确 URL 现在优先控制功能页。最终相关前端定向回归 **25 pass / 0 fail / 185 assertions**。
- 新版多浏览器页实机复查：另一页保持 `view=code`，当前页保持 `view=cli`，选中功能和 URL 一致。
- 当前最终控制面 `cs-control-plane:rfc008-20260920-3`，镜像 `ac32d99336fe89f08054ebbf1810833b60a1c3d6991d4e225e084546285151cf`，API／controller／session 均逐个更新并就绪。

## 发布收口

最终共享候选的 `bun run check` 已完成：**1684 pass / 5 skip / 0 fail，9534 assertions，1689 tests / 272 files / 228.33s**；架构、全仓 lint、根类型与工作台类型全部通过。日志 `/private/tmp/rfc008009-check-final-2.log`。此前未完成检查被并行页面引用／E2E 类型声明中断，修复由所属任务完成。

本轮只提交 RFC-008 的 60 个明确路径；共享 STATE 与 RFC 索引完整保留 RFC-009 输出。门禁执行期间 RFC-008 的 53 个代码／测试／设计附件文件指纹未变化；并行设置与其他门禁 WIP 不随本次代码提交。功能提交 `5252c4c43804bdfbd2a8beb92bc3c2f9289bb12d` 已推送到 `main`，推后 fetch 确认精确同步。

[GitHub CI 35502873316](https://github.com/wangbinquan/CrewStation/actions/runs/35502873316) 针对上述精确 SHA 已完成且 **success**：`check` 3 分 8 秒（含完整门禁和工作台构建），`e2e` 5 分 29 秒（新集群安装平台后真实浏览器验收）。T1–T7 与 DS-01…DS-16 完成，RFC-008 标记 **Done**。后续收口提交仅更新这份证据与 RFC／接力状态，不修改已验证的生产代码。

## 追加：会话历史滚动与创建入口命名（2026-09-20）

作者指出 OpenCode 历史无法滚动，并要求把创建入口改为「创建开发Agent会话」。

- 根因回归先红：`@xterm/addon-serialize@0.14.0` 保存 tracking 模式却丢失 SGR / SGR_PIXELS 鼠标编码，重连后滚轮与拖动事件格式改变。Runner 现在通过公开 parser 观察模式，快照完整保留编码；覆盖跨片段 ANSI、复位、组合模式和实际恢复后的模式查询。浏览器端只为缺失编码的旧 OpenCode 快照补 SGR，不覆盖明确保存的编码或其他协议。
- [OpenCode 1.18.29 原生界面](https://github.com/anomalyco/opencode/blob/v1.18.29/packages/tui/src/routes/session/index.tsx) 默认隐藏 session scrollbar；新建托管原生会话现在在 XDG 状态目录首次写入 `scrollbar_visible: true`，保留已有偏好，目录和文件可由 worker 读取。旧会话可在 Ctrl+P 中选择 `Toggle session scrollbar`。界面提示获取输入控制后回看历史。
- 真实创建按钮、中英文引导、空状态及设计附件统一命名。单击启动、双击去重与按原请求重试的行为保持原测试约束。设计稿显示独立滚动条：正文 141px / 内容 224px，真实拖动后 `scrollTop=82.5`，输入框保持窗底；这是设计附件证据，不是 Agent 端到端证据。
- 真实浏览器加载生产 `NativeTerminalSurface` 的无后端夹具：旧快照恢复后向上滚动得到 `ESC[<64;65;10M`，拖动包括按下 `ESC[<0;65;10M`、移动 `ESC[<32;65;17M` 和释放 `ESC[<0;65;17m`；新快照向下滚动得到 `ESC[<65;65;10M`；只读时滚轮与拖动输出均为空。该夹具未创建 Agent 或 PTY。
- 本机 console 镜像 `cs-console:rfc008-scroll-20260920` 已就绪。任务镜像摘要 `sha256:1afe7bbad507de9aa23f36e7a2be07049e2da4de4d98a335ef4173a3357147b9` 已进入默认档位 `volc-glm-5-2` 修订 3，模型、凭据与资源套餐保持。自动测试 `pft_01a0be574e3d70009c6b4957885d7ffa` 通过：镜像、Runner 协议 2、启动前步骤、CLI 1.18.29 与真实模型回文均成功，执行任务 `tsk_01a0be574f5270008f210fdaf009e5b8`。
- 新建真实 Agent 长历史验收被自动审批拦截：理由为尚需明确授权创建验证资源。已向作者询问创建 1 个验证会话、完成后停止，待答复；未绕过此限制，不能将自动模型回文或浏览器夹具说成原生长历史已实测。
- 运行时、驱动与终端组件定向回归 **47 pass / 0 fail / 190 assertions（6 文件）**，日志 `/tmp/cs-rfc008-scroll-core-final.log`；整页回归曾受并行 RFC-011 新角色守卫的在制夹具影响，所属任务补齐后复验 **14 pass / 0 fail / 103 assertions（2 文件）**，日志 `/tmp/cs-rfc008-scroll-navigation-final.log`。完整门禁第一次在并行 identity/release/k8s 的 lint 阶段停止，尚不能认定整个共享候选或本次 GitHub CI 通过。候选和剩余验证将在发布前继续核对。

### 全量检查追加记录

功能已固化为本地提交 `b69fef7280d5edf6314161f2a052db085bea4119`，新增可执行行覆盖 **67/67（100%）**。随后共享候选的架构、lint 与工作台类型检查通过；根类型检查期间 RFC-010/012 新测试仍在修正，未将中断的检查视为全绿。

完整测试自然结束：**1814 pass / 5 skip / 17 fail，9986 assertions，1836 tests / 295 files / 270.89s**，日志 `/tmp/cs-rfc008-scroll-tests-final.log`。其中两条属于本次修复的旧断言：失败终端的空画面现在显式携带默认鼠标编码、恢复页应查找新的创建按钮。已分别修正并复验：Runner 真实 WebSocket 协议 **11 pass / 0 fail / 126 assertions**，恢复页面 **6 pass / 0 fail / 42 assertions**。没有修改生产行为去迎合旧断言。

其他失败已按 RFC-010/011/012 归属交还原任务：新增 Pod UID／接口扫描、角色夹具与导航、项目档位配置断言，以及两个尚未部署的新管理页实机验收。它们保留在共享工作树，未删除、跳过或纳入本次代码提交。完整发布门禁与新建真实 Agent 的长历史验收仍待完成，不能将本地定向通过报告为远端 CI 或原生长历史实测通过。
